# Crear entorno virtual (en raiz)
python3 -m venv .venv

# Instalar dependencia
pip install -r requirements.txt

# PHP
## php server
para probar los endpoints del API de PHP hay que correr el server en la carpeta exacta del index.php
## terminal
usar php -S localhost:8000 en la carpeta del archivo

# probar peticiones POST, GET, PUT, etc con POSTMAN
POSTMAN permite todos los verbos para hacer peticiones http y testear
